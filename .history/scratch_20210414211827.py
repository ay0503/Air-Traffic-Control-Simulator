from airline_data import airlines

callsign = "DAL123"

airlines = dict()

airline = ''
for letter in self.callsign:
    if letter.isalpha():
        airline += letter
    airlines.get(airline)
    return airline
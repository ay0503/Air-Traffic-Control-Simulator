from objects import *
from airline_data import airlines
from airport_data import airports
import random

def randNo(random.choice([2,3,4])):
    return random.randrange(10 ** (len - 1), 10 ** len)

airline = random.choice(list(airlines.keys()))
no = randNo()))
